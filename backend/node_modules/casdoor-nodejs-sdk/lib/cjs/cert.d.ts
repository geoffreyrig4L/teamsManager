import { AxiosResponse } from 'axios';
import { Config } from './config';
import Request from './request';
export interface Cert {
    owner: string;
    name: string;
    createdTime: string;
    displayName: string;
    scope: string;
    type: string;
    cryptoAlgorithm: string;
    bitSize: number;
    expireInYears: number;
    certificate?: string;
    privateKey?: string;
    authorityPublicKey?: string;
    authorityRootPublicKey?: string;
}
export declare class CertSDK {
    private config;
    private readonly request;
    constructor(config: Config, request: Request);
    getCerts(): Promise<AxiosResponse<{
        data: Cert[];
    }>>;
    getCert(id: string): Promise<AxiosResponse<{
        data: Cert;
    }>>;
    modifyCert(method: string, cert: Cert): Promise<AxiosResponse<Record<string, unknown>>>;
    addCert(cert: Cert): Promise<AxiosResponse<Record<string, unknown>>>;
    updateCert(cert: Cert): Promise<AxiosResponse<Record<string, unknown>>>;
    deleteCert(cert: Cert): Promise<AxiosResponse<Record<string, unknown>>>;
}
