import { AxiosResponse } from 'axios';
import { Config } from './config';
import Request from './request';
export interface Enforcer {
    owner: string;
    name: string;
    createdTime: string;
    updatedTime?: string;
    displayName: string;
    description: string;
    model: string;
    adapter: string;
    isEnabled?: boolean;
}
export declare class EnforcerSDK {
    private config;
    private readonly request;
    constructor(config: Config, request: Request);
    getEnforcers(): Promise<AxiosResponse<{
        data: Enforcer[];
    }>>;
    getEnforcer(id: string): Promise<AxiosResponse<{
        data: Enforcer;
    }>>;
    modifyEnforcer(method: string, enforcer: Enforcer): Promise<AxiosResponse<Record<string, unknown>>>;
    addEnforcer(enforcer: Enforcer): Promise<AxiosResponse<Record<string, unknown>>>;
    updateEnforcer(enforcer: Enforcer): Promise<AxiosResponse<Record<string, unknown>>>;
    deleteEnforcer(enforcer: Enforcer): Promise<AxiosResponse<Record<string, unknown>>>;
}
