import { AxiosResponse } from 'axios';
import { User } from './user';
import { Config } from './config';
import Request from './request';
export interface Group {
    owner: string;
    name: string;
    createdTime: string;
    updatedTime?: string;
    displayName: string;
    manager?: string;
    contactEmail?: string;
    type?: string;
    parentId?: string;
    isTopGroup?: boolean;
    users?: User[];
    title?: string;
    key?: string;
    children?: Group[];
    isEnabled?: boolean;
}
export declare class GroupSDK {
    private config;
    private readonly request;
    constructor(config: Config, request: Request);
    getGroups(): Promise<AxiosResponse<{
        data: Group[];
    }>>;
    getGroup(id: string): Promise<AxiosResponse<{
        data: Group;
    }>>;
    modifyGroup(method: string, group: Group): Promise<AxiosResponse<Record<string, unknown>>>;
    addGroup(group: Group): Promise<AxiosResponse<Record<string, unknown>>>;
    updateGroup(group: Group): Promise<AxiosResponse<Record<string, unknown>>>;
    deleteGroup(group: Group): Promise<AxiosResponse<Record<string, unknown>>>;
}
