import { AxiosResponse } from 'axios';
import { Config } from './config';
import Request from './request';
export interface Plan {
    owner: string;
    name: string;
    createdTime: string;
    displayName: string;
    description: string;
    pricePerMonth?: number;
    pricePerYear?: number;
    currency?: string;
    isEnabled?: boolean;
    role?: string;
    options?: string[];
}
export declare class PlanSDK {
    private config;
    private readonly request;
    constructor(config: Config, request: Request);
    getPlans(): Promise<AxiosResponse<{
        data: Plan[];
    }>>;
    getPlan(id: string): Promise<AxiosResponse<{
        data: Plan;
    }>>;
    modifyPlan(method: string, plan: Plan): Promise<AxiosResponse<Record<string, unknown>>>;
    addPlan(plan: Plan): Promise<AxiosResponse<Record<string, unknown>>>;
    updatePlan(plan: Plan): Promise<AxiosResponse<Record<string, unknown>>>;
    deletePlan(plan: Plan): Promise<AxiosResponse<Record<string, unknown>>>;
}
