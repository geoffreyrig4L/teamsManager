import { AxiosResponse } from 'axios';
import { Config } from './config';
import Request from './request';
export interface Pricing {
    owner: string;
    name: string;
    createdTime: string;
    displayName: string;
    description: string;
    plans?: string[];
    isEnabled?: boolean;
    trialDuration?: number;
    application: string;
    submitter?: string;
    approver?: string;
    approveTime?: string;
    state?: string;
}
export declare class PricingSDK {
    private config;
    private readonly request;
    constructor(config: Config, request: Request);
    getPricings(): Promise<AxiosResponse<{
        data: Pricing[];
    }>>;
    getPricing(id: string): Promise<AxiosResponse<{
        data: Pricing;
    }>>;
    modifyPricing(method: string, pricing: Pricing): Promise<AxiosResponse<Record<string, unknown>>>;
    addPricing(pricing: Pricing): Promise<AxiosResponse<Record<string, unknown>>>;
    updatePricing(pricing: Pricing): Promise<AxiosResponse<Record<string, unknown>>>;
    deletePricing(pricing: Pricing): Promise<AxiosResponse<Record<string, unknown>>>;
}
