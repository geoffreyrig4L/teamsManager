import { AxiosResponse } from 'axios';
import { Config } from './config';
import Request from './request';
export interface Role {
    owner: string;
    name: string;
    createdTime: string;
    displayName: string;
    description: string;
    users?: string[];
    roles?: string[];
    domains?: string[];
    isEnabled?: boolean;
}
export declare class RoleSDK {
    private config;
    private readonly request;
    constructor(config: Config, request: Request);
    getRoles(): Promise<AxiosResponse<{
        data: Role[];
    }>>;
    getRole(id: string): Promise<AxiosResponse<{
        data: Role;
    }>>;
    modifyRole(method: string, role: Role): Promise<AxiosResponse<Record<string, unknown>>>;
    addRole(role: Role): Promise<AxiosResponse<Record<string, unknown>>>;
    updateRole(role: Role): Promise<AxiosResponse<Record<string, unknown>>>;
    deleteRole(role: Role): Promise<AxiosResponse<Record<string, unknown>>>;
}
