import { AxiosResponse } from 'axios';
import { Config } from './config';
import Request from './request';
export interface Subscription {
    owner: string;
    name: string;
    createdTime: string;
    displayName: string;
    startDate?: Date;
    endDate?: Date;
    duration?: number;
    description: string;
    user?: string;
    plan?: string;
    isEnabled?: boolean;
    submitter?: string;
    approver?: string;
    approveTime?: string;
    state?: string;
}
export declare class SubscriptionSDK {
    private config;
    private readonly request;
    constructor(config: Config, request: Request);
    getSubscriptions(): Promise<AxiosResponse<{
        data: Subscription[];
    }>>;
    getSubscription(id: string): Promise<AxiosResponse<{
        data: Subscription;
    }>>;
    modifySubscription(method: string, subscription: Subscription): Promise<AxiosResponse<Record<string, unknown>>>;
    addSubscription(subscription: Subscription): Promise<AxiosResponse<Record<string, unknown>>>;
    updateSubscription(subscription: Subscription): Promise<AxiosResponse<Record<string, unknown>>>;
    deleteSubscription(subscription: Subscription): Promise<AxiosResponse<Record<string, unknown>>>;
}
