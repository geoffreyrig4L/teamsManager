import { AxiosResponse } from 'axios';
import { Config } from './config';
import Request from './request';
export interface Token {
    owner: string;
    name: string;
    createdTime: string;
    application: string;
    organization: string;
    user: string;
    code: string;
    accessToken: string;
    refreshToken: string;
    expiresIn: number;
    scope: string;
    tokenType: string;
    codeChallenge: string;
    codeIsUsed: boolean;
    codeExpireIn: number;
}
export declare class TokenSDK {
    private config;
    private readonly request;
    constructor(config: Config, request: Request);
    getTokens(p: number, pageSize: number): Promise<AxiosResponse<{
        data: Token[];
    }>>;
    getToken(id: string): Promise<AxiosResponse<{
        data: Token;
    }>>;
    modifyToken(method: string, token: Token): Promise<AxiosResponse<Record<string, unknown>>>;
    addToken(token: Token): Promise<AxiosResponse<Record<string, unknown>>>;
    updateToken(token: Token): Promise<AxiosResponse<Record<string, unknown>>>;
    deleteToken(token: Token): Promise<AxiosResponse<Record<string, unknown>>>;
}
